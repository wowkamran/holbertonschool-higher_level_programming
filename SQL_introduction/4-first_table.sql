-- Script that creates a table called first_table
-- Table has columns: id INT, name VARCHAR(256)
-- Create table only if it does not already exist
CREATE TABLE IF NOT EXISTS first_table (
    id INT,
    name VARCHAR(256)
);
